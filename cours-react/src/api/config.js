const protocol = "https";
const host = "linkway.site"; //127.0.0.1
const webVpnPath = `${protocol}://${host}:5557/`;
const talkSocketPath = `${protocol}://${host}:5558`;

export {
    webVpnPath,
    talkSocketPath
};